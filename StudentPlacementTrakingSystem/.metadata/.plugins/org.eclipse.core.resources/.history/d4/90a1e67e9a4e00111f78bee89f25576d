package org.placepro.repository;

import java.util.List;
import org.placepro.model.Users;

public interface StudentRepository {
    void save(Users student);
    void delete(int id);
    List<Users> findAllStudents();  // only students, not admins
}
